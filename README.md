# Expense-Tracker
A command-line Expense Tracker built in Python using OOP principles. Supports adding, viewing, and deleting expenses, calculating totals by category, and persistent storage via JSON file handling — with exception handling for robust input validation.
